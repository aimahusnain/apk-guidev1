{
    "version": 2,
  
    "routes": [
      { "handle": "filesystem" },
      { "src": "/.*", "dest": "/index.ts" }
    ],
    "env": {
      "DATABASE_URL": "mongodb+srv://aimahusnain:aimahusnain@blogcluster.4rxblmo.mongodb.net/tests"
    },
    "build": {
      "env": {
        "VERCEL_ENV": "@vercel/env"
      },
      "scripts": {
        "build": "prisma generate && npm run build",
        "prerender": "npm run build"
      }
    }
  }
  